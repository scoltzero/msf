<!DOCTYPE PLUGIN [
<!ENTITY name "msf">
<!ENTITY author "luochuhan">
<!ENTITY version "0.6.0">
<!ENTITY githubRepo "scoltzero/msf">
<!ENTITY releaseTag "v0.6.0">
<!ENTITY packageName "msf-0.6.0-x86_64-1">
<!ENTITY packageFile "&packageName;.txz">
<!ENTITY packageURL "https://github.com/&githubRepo;/releases/download/&releaseTag;/&packageFile;">
<!ENTITY packageSHA256 "a3710d104959799f0f6261c2a14469e4d86ff3a349a6d2ae9e27d26b81ade239">
]>
<PLUGIN name="&name;" author="&author;" version="&version;" pluginURL="https://github.com/&githubRepo;/releases/latest/download/msf.plg" launch="Utilities/&name;" support="https://forums.unraid.net/topic/200222-plugin-msf-mosdns-and-mihomo-management/" icon="network-wired" min="6.12.0">
  <CHANGES>
    <![CDATA[
### msf 0.6.0

#### 独立界面、来源说明与发布基线

- 全面重做登录、初始化、主框架、导航、MosDNS、Mihomo、日志、配置和设置界面，统一页面标题、响应式布局、背景与动效，并针对长列表、图表和代理页面优化渲染性能。
- 增加 MosDNS 一键本地回环诊断，改善客户端模式选择、规则列表、配置编辑器、日志展示和 Mihomo 页面设置的使用体验。
- 将运行时模板目录从历史命名调整为中性的 `runtime_templates`，移除旧项目名兼容入口、已停用的 Mihomo 旧页面和未使用的第三方图标；保留运行所需的 MosDNS/Mihomo 配置与规则数据。
- 补全双语 README、第三方来源与许可证清单、UI 来源记录和品牌政策；MSF 自有 Mizar Logo 与动效继续作为项目标识，第三方组件和改编代码按各自许可证标注。
- 更新 Mihomo 默认路由与恢复逻辑，并保持 MosDNS、Mihomo、Zashboard 下载来源及摘要验证可审计。
- 将 Mizar 源文件、当前动效、QA 记录和正式导出统一整理到 `logo_motion_mizar/`，删除重复概念稿、旧版动效和过时 UI 验收资料；品牌生成脚本现在同步生成完整 SHA-256 清单。
- 按品牌、合规、Docker 和发布职责重组仓库脚本，删除未接入的前端页面、组件和 Hook，并清除 Go 后端不可达的旧诊断、下载、面板及更新辅助代码。
- 清理前端未使用依赖并显式声明直接依赖，发布候选的 `npm audit` 保持零已知漏洞。
- 本版本从同一个干净的 `main` tag 构建 Linux、Unraid、fnOS、macOS 和 GHCR 镜像；旧项目名兼容入口已经删除，依赖该入口的外部脚本需要改用 `msf` 命令和当前数据目录。

    ]]>
  </CHANGES>

  <FILE Name="/boot/config/plugins/&name;/&packageFile;" Run="upgradepkg --install-new --reinstall">
    <URL>&packageURL;</URL>
    <SHA256>&packageSHA256;</SHA256>
  </FILE>

  <FILE Run="/bin/bash">
    <INLINE>
      <![CDATA[
set -e
mkdir -p /boot/config/plugins/msf
cfg="/boot/config/plugins/msf/msf.cfg"
if [ ! -f "$cfg" ]; then
  cat > "$cfg" <<'CFGEOF'
ENABLED="yes"
HOST="0.0.0.0"
PORT="7777"
DATA_DIR="/mnt/user/appdata/msf"
CFGEOF
fi
chmod 0755 /etc/rc.d/rc.msf 2>/dev/null || true
chmod 0755 /usr/local/emhttp/plugins/msf/event/started 2>/dev/null || true
chmod 0755 /usr/local/emhttp/plugins/msf/event/stopping_svcs 2>/dev/null || true
if grep -q '^ENABLED="yes"' "$cfg" && [ -x /etc/rc.d/rc.msf ]; then
  nohup /etc/rc.d/rc.msf start >/dev/null 2>&1 &
fi
      ]]>
    </INLINE>
  </FILE>

  <FILE Run="/bin/bash" Method="remove">
    <INLINE>
      <![CDATA[
set +e
if [ -x /etc/rc.d/rc.msf ]; then
  /etc/rc.d/rc.msf stop
fi
removepkg msf-0.6.0-x86_64-1
rm -f /boot/config/plugins/msf/msf-0.6.0-x86_64-1.txz
echo "msf plugin removed. Persistent data remains under /mnt/user/appdata/msf unless you delete it manually."
      ]]>
    </INLINE>
  </FILE>
</PLUGIN>
